<!doctype html>
<html lang="{{ str_replace('_','-',app()->getLocale()) }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  @include('partials.seo')
  @includeIf("partials.seo")
  @includeIf("partials.jsonld")
</head>
<body>
  @include('components.lang-toggle')
  @yield('content')
</body>
</html>
