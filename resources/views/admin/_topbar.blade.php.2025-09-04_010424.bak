<div class="topbar">
  <button class="btn btn-outline-secondary btn-sm d-lg-none" data-toggle="sidebar">☰</button>
  <form class="search" method="GET" action="{{ url()->current() }}">
    <input class="form-control" type="search" name="q" value="{{ request('q') }}" placeholder="Search...">
  </form>
  <div class="dropstart">
    <button class="btn btn-light btn-sm dropdown-toggle" data-bs-toggle="dropdown">Admin</button>
    <div class="dropdown-menu dropdown-menu-end">
      <a class="dropdown-item" href="{{ route('admin.settings.index') }}">Settings</a>
      <div class="dropdown-divider"></div>
      <form method="POST" action="{{ route('logout.perform') }}">@csrf
        <button class="dropdown-item text-danger">Sign out</button>
      </form>
    </div>
  </div>
</div>
