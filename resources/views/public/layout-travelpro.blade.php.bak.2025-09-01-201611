<!doctype html>
<html lang="{{ str_replace('_','-', app()->getLocale()) }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  @includeWhen(View::exists('partials.meta'), 'partials.meta')
  @includeWhen(View::exists('partials.security_meta'), 'partials.security_meta')
  <link rel="icon" href="{{ asset('images/favicon.ico') }}">

  {{-- TravelPro CSS (adjust filenames if different in public/assets/css) --}}
  <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
  <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
  @stack('head')
  <link rel="stylesheet" href="{{ asset('css/theme.css') }}?v={{ filemtime(public_path('css/theme.css')) }}">
</head>
<body class="public travelpro">
  @includeWhen(View::exists('partials.header-public'), 'partials.header-public')
  <main>@yield('content')</main>
  @includeWhen(View::exists('partials.footer-public'), 'partials.footer-public')

  {{-- TravelPro JS --}}
  <script src="{{ asset('assets/js/bootstrap.bundle.min.js') }}"></script>
  <script src="{{ asset('assets/js/main.js') }}"></script>
  @stack('scripts')
  @include('public._analytics')
</body>
</html>
