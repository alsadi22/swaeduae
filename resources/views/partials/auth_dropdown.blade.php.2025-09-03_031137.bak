@auth
  <a class="dropdown-item" href="{{ route('my.profile') }}">My Profile</a>
  <form method="POST" action="{{ route('logout.perform') }}" class="dropdown-item p-0 m-0" style="display:block">
    @csrf
    <button type="submit" class="w-100 text-start" style="background:none;border:0;padding:0;margin:0;cursor:pointer;">Logout</button>
  </form>
@else
  <div class="dropdown-divider"></div>
@endauth
