<header class="header-area">
  <div class="container">
    <div class="header-inner d-flex align-items-center" style="gap:16px; min-height:64px">
      <a class="logo d-flex align-items-center" href="{{ url('/') }}" aria-label="SwaedUAE">
        <img src="{{ asset('assets/travelpro/theme_public/assets/images/adventure_logo.svg') }}"
             alt="SwaedUAE" style="height:38px">
      </a>

      <nav class="main-menu ms-auto" aria-label="Primary">
        <ul class="d-flex" style="list-style:none;margin:0;padding:0;gap:18px">
          <li><a href="{{ url('/') }}">Home</a></li>
          <li><a href="{{ url('/opportunities') }}">Tours</a></li>
          <li><a href="{{ url('/about') }}">About</a></li>
          <li><a href="{{ url('/contact') }}">Contact</a></li>
        </ul>
      </nav>

      <div class="nav-right d-flex align-items-center" style="gap:8px">
        @auth
          @if (Route::has('logout'))
            <a href="{{ url('/my/profile') }}" class="btn btn-outline-secondary btn-sm">My Profile</a>
            <form class="d-inline" method="POST" action="{{ route('logout') }}">@csrf
              <button type="submit" class="btn btn-outline-secondary btn-sm">Sign out</button>
            </form>
          @else
            <a href="{{ url('/my/profile') }}" class="btn btn-outline-secondary btn-sm">My Profile</a>
          @endif
        @else
          <a href="{{ route('login', [], false) }}" class="btn btn-outline-primary btn-sm">Sign in</a>
          <a href="{{ route('org.login', [], false) }}" class="btn btn-outline-secondary btn-sm">Organization</a>
        @endauth
      </div>
    </div>
  </div>
</header>
